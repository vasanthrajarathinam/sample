import { TrippledotPipe } from './trippledot.pipe';

describe('TrippledotPipe', () => {
  it('create an instance', () => {
    const pipe = new TrippledotPipe();
    expect(pipe).toBeTruthy();
  });
});
